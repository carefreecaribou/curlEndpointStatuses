# Get Endpoint Status Using Curl
Create an endpoints.txt file that contains the endpoint URLS you wish to query. Use one endpoint per line.

# Example Usage

./curl.sh endpoints.txt > endpointStatuses.txt
